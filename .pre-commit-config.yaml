repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.6
    hooks:
      # Run the linter (checks for logic bugs and auto-fixes simple issues)
      - id: ruff
        args: [--fix]

      # Run the formatter (enforces consistent spacing and style)
      - id: ruff-format